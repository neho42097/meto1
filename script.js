// Example of a very basic (and insecure) hardcoded check
const correctPassword = "mySecretPassword";
if (enteredPassword === correctPassword) {
    // Login successful
} else {
    // Password wrong
)